\documentclass[12px]{article}
\usepackage{graphicx} % Required for inserting images

\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english, russian]{babel}
\usepackage{amsmath}

\usepackage{geometry}
 \geometry{
 a4paper,
 total={170mm,257mm},
 left=20mm,
 top=20mm,
 }

\title{ЭВМ}
\author{VG6}

\begin{document}

\maketitle

\section{До этого мне было лень}
\section*{Вопросики (23 октября 2025)}
Какой принцип Неймана позволяет привесить скорость света?\\
Вычислительный процесс следует организовывать параллельным образом.\\
Почему ЭВМ в двоичной системе счисления, а не в десятичной?\\
5 принцип Неймана: разработка устройств для других операция нецелесообразно (кроме сумматора). В теории он прав, а на практике нет. К примеру аппаратные умножители. \\
\textbf{Главный принцип Неймана}: и программа и данные лежат в одном и том же месте в одном и том же виде. Не нужно для заменять машину, можно заменить только программу. \\
\textbf{Схема} - графическое обозначение элементов и связи между ними. \\

\section{Структура классической ЭВМ.}
Состоит из 3х блоков.
\begin{center}
\includegraphics[width=0.7\linewidth]{images/Схема ЭВМ.png}
\end{center}

\subsection{Описание блоков.}
\begin{enumerate}
    \item Арифметико-Логическое устройство
    \begin{itemize}
        \item Выполняет преобразование информации представленной в двоичном виде. Например: числа при выполнении арифметической операции. 
        \item \textbf{Операнд} - мы будем называть участника арифметической или логической операции. 
        \item АЛУ преобразует операнды и получает результат и признаки результат (дополнительные сведенья о том, какй результат получился Правильный/Неправильный).
        \item Какой операцией заняться определяют управляющие сигналы.
        \item \textbf{Операция} - арифметическое или логическое действие выполняемое в АЛУ над операндами. То, что может делать АЛУ называется операцией.
    \end{itemize}
    \item Запоминающее устройство
    \begin{itemize}
        \item Предназначение: хранить двоичные числа.\\
        Для этого нужны 3 операции:
        \begin{enumerate}
            \item Запись
            \item Чтение
            \item Выборка (адресация не совсем одно и тоже)
        \end{enumerate}
        \item Запоминающее устройство классической ЭВМ является адресным устройством. Каждое число хранится в ячейке имеющей свой уникальный адрес, который тоже является числом. В простейшем случае - порядковый номер ячейки памяти.
        \item Запоминающее устройство представляет собой одномерный массив, где в качестве индекса выступает адрес (номер ячейки).
        \item 4 действия (тоже + хранение) определяется управляющими сигналами, среди которых сигналы: операции записи, чтения и многое другое. 
    \end{itemize}
    \item Устройство Управления
    \begin{itemize}
        \item Занимается управлением вычислительным процессом. Тоесть его выходом является порождение управляющих сигнаов (сигналов управления), которые потребляются другими ящиками и самим собой. 
        \item Помимо внешних блоков он управляет и самим собой.
        \item Цели и задачи управления: реализация выполнения команды. Для этого формируемые им сигналы несут 2 сущности: что делать, когда это надо сделать.
        \item Эту команду откуда-то надо взять. А они лежат в запоминающем устройстве, там же, где и данные. 
        \item На выходе сигналы, на входе ... из памяти
        \item нужно формировать адресс ячейки откуда взять команду, получить её и выполнить.
    \end{itemize}
\end{enumerate}
Связи между этими блоками изображены на рисунке.
По признакам результата УУ влияет на дальнейшее прохождение вычислительного процесса (изменять последовательность выполнения команд).\\\\
Например, произошло переполнение, значит нужно сформировать признак переполнения и передать УУ.\\\\
УУ получает команды из памяти, однако и операнды и команды лежат в разных ячейках запоминающего устройства (адресам) поэтому УУ должен формировать адреса как команд, так и операнд.
\subsection{Цикл выполнения команды.}
Структура организована циклически. И этот бег по кругу является бесконечным повторением. \\

\subsection{Память. Запоминающее устройство.}
Строится по иерархическому приципу.
\textbf{Регистр} - запоминающее устройство ёмкостью в 1 число. Есть 2 типа:
\begin{enumerate}
    \item Специальзированные регистры, функции которых предопределены конструкцией ЭВМ и являются неизменными и регистры 
    \item Общего назначения, функционал которых может предопределятся. Доступны для программистов.
\end{enumerate}
\subsubsection{Виды памяти}
\textbf{Сверхоперативные ЗУ} - обычно безадресного типа. К таким устройствам относятся буферные запоминающие устройства, стек.\\
\textbf{Буфер} - запоминающее устройство между ЗУ с разной скоростью работы для сглаживаний по времени. Обычно организуется очередью (FIFO).\\
\textbf{Стек} - первым вылетает последний заряженный патрон. (LIFO). История перехода к подпрограммам. Используется в системе прерываний.\\
\textbf{Постоянное запоминающее устройство (ROM)} - адресное запоминающее устройство без функции записи.
\textbf{EPROM} - оставляет возможность сменить содержимое путём перезаписи, что требует специальное устройство (программатор).\\
\textbf{КЭШ L1, L2, L3, L4} - безадрессное ассоциативное запоминающее устройство. Небольшая ёмкость, но кратно ускоряет скорость работы вычислительного устройства. Благодаря ассоциативному доступу (тэгами) аккумулируют в себе те команды или данные, которые используются наиболее интенсивно. Тем самым создавая копию ячеек Оперативной памяти кратно уменьшают доступ к этим данным.\\
\textbf{\textit{Основная оперативна память ООП}, ОЗУ, RAM, память с произвольной выборкой, память ЭВМ} - (синенькое на схеме) память, в которой хранятся те самые команды и данные по Нейману.\\
\textbf{Специализированные блоки памяти} - обмен между вычислительным ядром внешним миром (многопортовая память, ассоциативные ЗУ (используется в КЭШе и т.д. при поиске не по адресу, а по признаку), видеопамять)\\
\textbf{Внешние запоминающие устройства} - то, что подключается к ... через интерфейс. (..., облачные зранилища, Data-центры, ...)

\subsection{ОЗУ. Оперативное запоминающее устройтсво.}
Совокупность ячеек, пре
данный вид памяти для работы в качестве основной опреративной памяти ЭВМ дляжно обладать свойством произвольной выборки. \\

\textbf{Памятью с произвольной выборкой} мы будем называть адресное ЗУ, время выборки которого не зависит от адреса ячейки и последовательности обращений к ячейкам этого устройства.\\

Технические характеристики ЗУ: 
\begin{itemize}
    \item 1 бит - 1 двоичный разряд
    \item 1 байт - 8 бит. Попытка представить символы алфавита при помощи таблички кодирования (ASCII).
    \item К(Кило) - $2^{10} = 1024$
    \item М(Мега) - $2^{20} = ... $ 
    \item Г(Гига) - $2^{30} = ... $
\end{itemize}

\textbf{Память} - информационная ёмкость 1 адресуемой ячейки того, что имеет адрес. \\

\textbf{Организация ЗУ} - произведение числа ячеек на их разрядность, например: 4Гx8\\
Характеристики запоминающего устройства - временные.\\

\textbf{Быстродействие (производительность) ЗУ} - оценивают временами считывания и записи, длительностью циклов считывания/записи

\textbf{Время считывания} — интервал времени между моментом сигнала на считывание (адрес или разрешение считывания) и моментом выдачи данных на выходы памяти.

\textbf{Время записи} - интервал времени после задания сигнала записи, достаточный для установления ячейки в состояние, задаваемое входными данными.

\textbf{Цикл считывания/записи} - минимальный интервал между последовательными обращениями.
\subsection{Примеры условных графических обозначений (УГО ЗУ)}
Запоминающее устройство:
\begin{center}	
	\includegraphics[width=0.7\linewidth]{images/УГО ЗУ.jpg}\\
\end{center}
\textbf{n} - разрядность шины адреса (количество проводов). \\
\textbf{A} - адрес.\\
\textbf{DI} - Data Input. m - размер слова этого запоминающего устройства.\\
\textbf{WR} - Write Read. Какую операцию выполнять\\
\textbf{CS} - Chin Select. Вход разрешения работы этого модуля. Если на схеме выколотая точка, то это означает, что разрешающий сигнал "0".

Организация памяти: $2^n * m$

\section{Семинар 2.}
\subsection{Вопросики (30.10.2025)}
Из скольки частей состоит классическая ЭВМ? - из 3х\\
Какое волшебное слово объясняет, как эти 3 кубика работают? - циклически\\
Предназночение АЛУ? - выполнять АЛ операции.
Что порождает АЛУ? - признак результата и результат. 
что делать - статический сигнал\\
когда делать - импульс\\
Адресуема только вся ячейка, так что любая опреация порисходит сразу со всей ячейкой.\\

\subsection{Продолжаем УГО ЗУ}
DI и DO абсолютно одинаовые\\
Если сигнал на CS не совпадает с разрешающим, то устойство находится в стостоянии хранения. 
\subsection{Организация полупроводникового ЗУ с произвольной выборкой}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Полупроводниковое ЗУ.jpg}
\end{center}

Плоская двумерная матрица из запоминающих элементов. 
Бистабильные элементы. \\
Организованно так, чтобы можно было обратиться к люьому элементу. \\
\textbf{Дешифратор} - электронное устройство, преобразующее позиционный двоичный код в код унитарный. \\
\textbf{Унитарный} - двоичный код, содержащий 1 единствуенную единицу. Значение определяется номером разряда, в котором находится единица.\\
Таким образом мы получаем указатель этой единицей на елемент строки или стобца.\\
Регистр хранит 1 число - адрес, которое разделяется на 2 числа пополам, которые считаются номером адреса строки и номером адреса столбца.\\

\subsubsection{Усилители записи считывания}
Правильнее сказать: формировательи.\\
В зависимости от того, из чего сделаны элементы хранений могут быть разные потребности в вольтах и амперах. И суть этих устройств в согласовании внутренних сигналов и внешних стандар	тных логических сигналов. \\

\subsection{Временная диаграмма работы ЗУ RAM.}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Временная диаграмма.jpg}
\end{center}

Временная диаграмма - изображение на осях времени электрических сигналов несущих, значения логических переменных. \\

\subsubsection{Запоминающее устройство на фееритовом сердечнике}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Кольцо.jpg}
\end{center}

Это сплав железа и чем-то ещё так, чтобы получилось хорошо (речь о магнитных свойствах) и испекли пирог в виде кольца. Это кольцо обладает способностью быть намогниченным в двух направлениях
Вокруг тока образуется по правилу буравчика направление намагничивания, а после того\\

Функции:
\begin{enumerate}
	\item запись ... (всё те же 4)
\end{enumerate}
Запись:\\
Мы пустим половину тока по 1 проволке, а второю половину по второй. (только полный ток перемагнитит). Там где токи пересекаются ячейка переманичеваетя, а другие кольца даже не почешутся. 

Чтение:\\
Пустим 3-ю проволочку (справа кривулька), не пуская по ней ток. 
\begin{enumerate}
	\item Без изменения намагничивания вольтметр ничего не покажет.
	\item При изменении намагничивания возникнет импульс, вольтметр зафиксирует это.
\end{enumerate}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Кольцо (слайд).jpg}
\end{center}

Свойства: 
всё равно на радиацию. Разрушить можно только кувалдой. 
\subsection{Ячейка памяти с произвольной выборкой статического типа}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/SRAM.jpg}
\end{center}

Пара ... выход одного соединён со входом другого. 
Исключает возможность прямого тока от шины питания к земле. Реализован на полевых транзисторах. Хранит только до тех пор, пока есть питание. 
Функции выборки: M5, M6. Они подключены к плечам ячейки и будучи открыты шиной выборки соединяют выбранную ячейку с усилителем, подавая туда 2 сигнала противоположной полярности.\\

Зелёненькое и красненькое на слайдах меняется, поэтому биистабильно :)))\\

Здесь плохо, что ячейка хранит очень мало. 6 транзисторов на 1 бит.\\
Альтернитивный вариант динамическое запоминающее устройство на полевом транзисторе. 

\subsection{Одно-транзисторная ячейка памяти с произвольной выборкой динамического типа.}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/DRAM.jpg}
\end{center}

Можно использовать конденсатор в качестве элемента хранения.
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Кондей и полевой транзистор.jpg}
\end{center}
Эквивалентная схемма с открытым транзистором и подключённый к усилителю, а затем закроем транзистор. Если опять открыть, то заряд потечёт в усилитель.\\
Но так как это полуизолятор мы можем повесить дополнительное сопротивление и со временем заряд стечёт и информация будет потеряна. Чтобы этого не произошло нужно считать её, записать и делть это постоянно. \\
Тоесть хранение информации в такой ячейке происходит динамически и требует постоянного чтения и записи (регинирации информаии). 
Почти 90\% всем ЗУ устроены так. \\
Разреженная регинерация - регинерация не всего массива, а только тех, кто нужен. Любое обращение к ячейке вызывает регинирацию.
\\ \\
\section{Лекция 10 от 06.11.2025.}
Вопросики:\\
Чем отличается память от склероза? В том, что на экзамене от всё забудет :(\\
Какие 4 операции есть в ЗУ? \\
В каком виду ЗУ в процессе хранения необходимо слово выборка? - Регинирация памяти. Динамическая память. \\
Что такое регенирация? - чтение и обратная запись того, что прочли.\\
А каком виде ЗУ так же требуется записывать после простения? - В ЗУ на основе фееритового сердечника из-за разрушающего свойства. \\
В какой метрике среда хранения полупроводникового ЗУ с произвольной выборкой? - двумерный массив. \\
Что такое хорошо, а что такое плохо, когда речь о ячейке статического типа? Весь тот гемор с обращением динамически к памяти не нужен. \\
Чем больше ёмкость тем дольше он будет хранить в цикле регинерации. \\


\subsection{АЛУ}
\begin{center}
	\includegraphics[width=0.5\linewidth]{images/АЛУ.jpg}
\end{center}

Выполняет арифмитические и логические операции. Для арифмитеческих операций необходимо (по Нейману) иметь сумматор, но могут быть и дополнительные ускорители процессов вычисления. \\
Минимальное число сумматоров, которые необходимы для работы ЭВМ - 1.\\

Для логических же операций необходимы:
\begin{itemize}
	\item Логические элементы (минимально нуже базис)
	\item Дополнительное оборудывание, которое могло бы выбрать какую из операций необходимо выполнить. 
\end{itemize}

Операции АЛ выполняются над \textbf{операндами} - число участвующее в А или Л опреации.\\
Схемма сумматора не обладают свойством сохранять свой состояние в отличие от ЗУ. \\
Комбинационная схема - схема значение которой есть только тогда, когда на вход поданы элементы. \\

Необходимо добаыить устройтсво для хранения операндом размером 1 (регистр). На схеме это ригистры операнда A и B. \\
Выходы регистров подключены к входам блока операций, а входы к тому месту где живут операнды (в памяти) соединяются магистралью с ЗУ. \\
Результаты хранятся в памяти (записывается из регистра результата). 

Дополнительная обратная связь между регистром результата и ... позволяет использовать результат в следующем вычислении. Появляется регистр аккумулятор (накапливающий). \\
Нужно указать АЛУ какую операцию выолнить. Это указание несёт в себе \textbf{коп} - код операции - число определяющее номер операции в списке возможных операций. 

Признаки результатов не нарисовали и мстное устройство управления ( в это не лезем ). \\

\subsection{Одноразрядный полный комбинационный сумматор}
	\includegraphics[width=0.5\linewidth]{images/Full adder.jpg}
% \includegraphics
Нужно сделать из него сумматор многоразрядный последовательный сумматор.
\subsection{Многоразрядный последовательный сумматор}
	\includegraphics[width=0.5\linewidth]{images/Многоразрядный сумматор.jpg}
% \includegraphics
ВОкруг обычного сумматора распологаются 3 регистра. \\
РР - регистр результаты. \\
Однако идея данного устройтсва в том, что это регистры обладающие функцией сдвига кода. \\
Картинка с нарушениями нужна только для демонстрации. \\
Сдвиги происхлдят одновременно во всех 3 регистрах из-за управляющего сигнала. \\
Тригер типа D передаёт в следующий такт то, что было на его входе в теущем (D - delay). \\
Все имеют вход с буквой С - вход для сингаринизации. Подаётся сгнал синхронизации на все элементы которые должны выполнить действия одновременно. \\
Хорошо: объём оборудования весьма скромный ( 1 комбинационный сумматор и несколько регистров с функцией сдвига + триггер). \\

Плохо: Время выполнения суммиорования прямо записит от количества операндов (8 тактов для байта). 


\subsection{4-х разрядный комбинационный сумматор.}
	\includegraphics[width=0.6\linewidth]{images/4-ч разрядный сумматор.jpg}\\
Это уже кусочек схемы. Входы слева, выходы справа. Логическое распростронение сигналов слева направо, сверху вниз.
Для каждой пары разрядов свой собственный отдельный одноразрядный комбинационный сумматор. Так что вычисление суммы происходит одновременно за 1 такт. \\

Плохо: с переносом проблема. 

\section{Устройство управления}
Что делает? - Пораждает сигналы управляющие. \\
Из чего пораждает? - Исходя из программы. \\
Программа - алгоритм решения задачи, записанный с помощью команж ЭВМ. \\
Алгоритм - последовательность действий необходимые для решения задачи.\\ 
Устройство Управления формирует управляющие сигналы исходя из того, какую команду оно выполняет. Суть - выполнение команды программы. \\
Команда программы - число. 

Что такое формат? - смысловое значение присвоенное отдельным разрядам в двоичной записи. В нашем случае команды. \\
\includegraphics[width=0.5\linewidth]{images/Command Format.jpg}
\includegraphics[width=0.5\linewidth]{images/Command Format2.jpg}

\begin{enumerate}
	\item  Операционная часть
	\item Адресная часть 
\end{enumerate}

Всякая команда состоящая из этих частей имеет некоторый набор характеристик. 
Операционная часть команжы содержит в себе:
\begin{itemize}
	\item КОП (Код ОПерации - двоичное число идентифицирующее команду в системе команд ЭВМ) 
\end{itemize}

Какая связь между кодом операции и количество разрядов кода опреации: $2^n$ - Мощность .
Адресная часть:
\begin{enumerate}
	\item Адреса операндов, участвующих в этой команде. 
\end{enumerate}

\textbf{Адресация} - способ указания местаположения этого операнда в адресном поле команды.Способов этих можно придумать бесконечно. \\
\textbf{Основные способы адресации:}
\begin{enumerate}
	\item Когда адреса нет вообще. Неявная адресация. Нет указания куда помещать результат.\\
	\includegraphics[width=0.5\linewidth]{images/Adressing1.jpg}

	\item Непосредственная адресация. В адресном поле распологается сам операнд. \\
	\includegraphics[width=0.5\linewidth]{images/Adressing2.jpg}\\
	Чем команда меньше, тем быстрее она будет извлекаться. 
	\item Прямая адресация. 
	\item Регистровая адресация
	\includegraphics[width=0.5\linewidth]{images/Adressing3.jpg}\\
	\item Косвенная адресация\\
	\includegraphics[width=0.5\linewidth]{images/Adressing4.jpg}\\
		Адрес адреса операнда. Может быть построена на нескольких уровнях вложенности. 

\end{enumerate}


\section{Лекция 11. От 13.11.2025}
\subsection{Вопросики}
Что такое формат команд? - 2 части: операционная и адресная. В операционной - код операции (двоичное число, которое однозначно идентифицирует команду в системе команд)\\
С какой характеристикой ЭВМ связаня величина поля ввода операции? - Мощности системы команд эвм, что равно количеству команд в штуках.\\
Какая характеристика позволяет оценить количество полей в адресной части команды? - количество адресов (адресность)\\
Как называется то, что определяет способ записи местонахождения операнда в этом адресном поле - адресация или способ адресации. \\
Где следует искать операнд если использована непосредственная адресация - непосредственно в этом адресном поле. \\
А если адресация прямая - искать в оперативной памяти по адресной ячейки, который написан в адресном поле команды. \\
А если косвенное - искать адрес операнда в ячейке адрес которой указан в адресном поле команды. Адрес адреса операнда. \\
Поругайте мне косвеную адресацию - многократное обращание в памяти ( время докапывания до операнда (минимум 2 раза)). \\
О какой характеристики ЭВМ можно судить о величине адресного поля при использовании косвеной адресации - по объёму оперативной памяти.\\
Как сохранить косвеность и справиться с проблемами косвеной адресации? - использование варианта косвенойадресации, в котором хранится информация не в ячейке ОП, а в регистре, получается косвеная регистровая адресация. \\

\subsection{Регистровая адресация}
\begin{center}
	\includegraphics[width=0.5\linewidth]{images/Adressing5.jpg}
\end{center}
Скороть наивысшая. \\
1 обращение в регистровую память, 2 обращение по адресу, извлечённому из регистро обращаемся в ООП. Получаем все плюшки косвеной адресации. 

\subsection{относительная адресация}
\begin{center}
	\includegraphics[width=0.5\linewidth]{images/Adressing6.jpg}
\end{center}
Местоположение операндов в памяти задаётся с помощью смещения относительно чего-то. Обычно относительно некоторого базового адреса. Т.о. получается, что при формировании адреса операнда используется суммирование, а именно сумма адреса базового и смещение. \\
Крестик на фото и есть суммирование. 

СК - счётчик команд - хранит следующую команду. Местонахождение операнда определяется относительно местонахождением команды. 
В момент, когда надо загрусить команду в память задаётся начальное смещение в виде точки, куда будет загружаться программа - классика при мультипрограмном режиме. 

\subsection{Автоинкриментная адресация}
\begin{center}
	\includegraphics[width=0.5\linewidth]{images/Adressing7.jpg}
\end{center}
Не что иное как косвенная/косвенная регистровая адресация, у которой адрес операнда может меняться путём увеличения инкримента на константу при каждом использовании этой команды.\\
Т.о. при запуске такой каманды в цикле получим последовательное обращение подряд идущих элементов (нужно например для массивов). \\
Существует в варианте инкримента и декримента - одно увеличивает, другое уменьшает. 

\subsection{Заключение по видам адресаций}
Адресаций может быть бесконечно много. Нужно знать неявную, прямую, косвеную, регистроваю, косвеную регистроваю, автоинкримент декримент, относительную адресации. 


% Команда должна привести к набору управляющих сигналов. 

\subsection{Трёхадресная ЭВМ}
\begin{center}
	\includegraphics[width=1.0\linewidth]{images/3AdressEVM/3adress}
	\includegraphics[width=1.0\linewidth]{images/3AdressEVM/3adressBW}
\end{center}
Состоит из:
\begin{enumerate}
	\item Арифметико-Логическое Устройство. В нём логические элементы. На вход принимает операнды и пораждается результат (РР) и признок результата (РПр). 
	\item Оперативное Запоминающее Устройство. Функции: хранение, чтене, запись, выборка. 
	\item Устройство Управления. \\
	Оно разделяется на 2 основные часи:
	\begin{enumerate}
		\item РК - Регистр команды. Обращаем внимание на то, что речь про 1 и тоьлко 1 команду. \\
		Число, которое хранится в этом регистре представляет собой команду. Формат этого числа - формат команды. \\
		Структура регистра команды определяется опаратной реализацией формата команды. Число записанное туда как раз и занимает поля формата команды и содержит всё что там полагается. Могут обитать 3 адреса: a, b, c, а также сам Код ОПерации. \\
		Команд много, а регистр может хранить 1 единственную программу - выполняемую в данный момент времени. Именно по этой причине \textbf{Функция регистра команды}: хранение команды в процессе её выполнения (той самоё 1 единственной). \\
		Логичные вопросы, которые возникают:
		\begin{itemize}
			\item Как команда попадает в регист команды?
			\item Как команды в памяти превращаются в программу?
		\end{itemize}
		\textbf{Программа} - алгоритм для решения задачи, записанный командами ЭВМ. \\
		
		\textbf{Способ адресации команд программы} (не путать со способами адресации операндов) по сути способ связовании команд в программу: 
		\begin{enumerate}
			\item \textbf{Принудительная адресация программ команд программы.} \\
				Суть заключается в том, что команда в своей адресной части имеет адресное поле с адресом следующей команды (обычно где-то в конце). Получается, что текущая команда выполняемая в данный момент несёт в себе адрес следующей программы. Получается структура, которая называется \textbf{связанный список} (блок-чайн без шифрования ;) ).\\ 
				Плюсы и минусы такого способа:
				\begin{itemize}
					\item Никаких усилий для вычисления адреса следующей программы не нужно. команды могут распологаться как угодно в любых ячейках памяти. 
					\item Занимает место. Приходится тащить поле размером с адрес основной оперативной памяти, а команд много, так что количество необходимой памяти сильно увеличивается. 
				\end{itemize}
		\item \textbf{Естественная адресация команд программы.} \\
			Берётся память (одномерный массив ячеек или же вектор) и в неё последовательно в том порядке, в котором они должны выполняться вносятся команды. При таком способе к конструкции добавляется устройство, хранящее адрес команды и обладающее возможностью инкремента для добавления некоторой $\Delta$, что является размером данной команды, - это называется \textbf{счётчик команд}. \\
			Плюсы и минусы такого способа:
			\begin{itemize}
				\item Избавляемя от поля для команды, значит не приходится тратить лишнюю память.
			\item Нужно класть команды в ячейки строго в порядке выполнения и необходимо потратить дополнительное оборудование для формирования адреса следующей команды + небольшое время добавления дельты к адресу. 
			\end{itemize}
		\end{enumerate}
		Преимущество в размере команды преволирует над первым способом, поэтому обычно используется естестенная адресация. \\
		В трёхадресной ЭВМ используется именно естественная адресация (видно счётчик команд ;) )\\

		\textbf{Счётчик команд} - Instruction Pointer - IP - указатель на команду.
		\begin{center}
			ОЧЕНЬ ВАЖНО! Никакой связи IP с интернет протоколами на зачёте нет.
		\end{center}
		
		Счётчик команд передат адрес следующей команды на Регистр Адреса ЗУ, указывая на ячеёку памяти, где она хранится и считанное по этому адресу число загружается в Регистр Команды. 
		\item Управление сигналами: \\
			Помним, что Устройство Управления решает 2 задачи: что делать и когда это делать.\\
			Что делать понятно из команды, расположенной в регистре команд, код операции которой передаётся в дешифратор кода операции. Смысл этого действия в том, чтобы сообщить той части, которая формирует сигналы (Блоку Управления ОПерациями), какая это команда. \\
			
			Вопросом коaгда занимается Датчик Сигнала (служба времени было-бы красивее @Новиков). 
\begin{center}
	\includegraphics[width=0.5\linewidth]{images/3AdressEVM/Time}
\end{center}
			Что такое время с точкизрения физики? \\
			Время - это физическая величина, которую не определить, но оно обладает свойствами: время течёт равномерно и непрерывно. \\
			Заменяем физическое время на \textbf{время дискретное}.\\
			Для этого мы испльзуем генероватор квантов (сигналов) и предствляем время при помощи импульсов. \\
			\textbf{Импульс} - изменение во времени состояние электрического сигнала несущего значение логической переменной из исходного состояние в противоположное. \\
			Пучть 1 импульс будет равен 1 \textbf{кванту времени} или же по другому 1 \textbf{такту}, тогда другой импульс будет другим тактом и т.д.\\

			А между импульсами ничего нет. Тогда в нашем новом дискретном времени все события происходят в этих тактах. Поэтому эти такты мы можем пронумеровать (t - сейчас, t+1 - следующий момент времени, t + 2, ...). Можно и в обратную сторону (t - 1, t - 2, ...). \\
			
			Порадив такое квантовое время и поняв, что события происходят только в тактовый импульс мы становимся хозяевами того, что происходит в каждый конкретный импульс. Но физическое время никуда не делось и в нём происходят импульсы, а раз они формируются в физическом времени, то у них есть тот же небор характеристик, что и у импульсного сигнала: 
			\begin{itemize}
				\item Длительность
				\item Амплитуда
				\item Фронт
				\item Период ($T$)- время между одинаковыми частями 2 соседних импульсов. 
				\item Частота (тактовая частота) - величина обратная периоду: $F = \frac{1}{T}$. Её порождает генератор тактовых импульсов.\\
			Изменяя частоту мы изменяем насколько часто будут происходить события в нашем устройстве. Чем быстрее тактовая частота тем быстрее работает устройство.\\
			\end{itemize}

	Мы должны иметь возможность внутри циклы выполнения команды указывать на моменты времени, в которые должны выполняться те самые действия, которые будут повторяться. Т.о. мы должны не только разбить время на кванты, но и имет возможность выбирать (наверное тут было: какой квант выполнять). В этом помогает \textbf{распределитель импульсов цикла}.  

\begin{center}
	\includegraphics[width=0.5\linewidth]{images/3AdressEVM/TimeDiagram}
\end{center}
	
	В этом примере мы получили, что у нас есть 4 сигнала, которые связаны с друг с другом так, что импульсы на тактах 1, 2, 3, 4 будут соответствовать ...\\
	Подавая с линии A выполнится 1 такт, а на линии C - 3 такт. \\

	Схематехнически это выглядит так:
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/3AdressEVM/TimeDiagram2}
	\end{center}
	\end{enumerate}
	Вот тут у меня плохо получилось:\\
	Каждый тактовыё импульс изменяет состояние счётчика на +1. 
	Дешифратор преобразует позиционный код в унитарный. \\
	Пришёл импульс, счётчик переключился, одразовался один ноль (единица двоичная) единичка на дешифраторе переместилась на 1 выход. След на дешифратор, счётчик на 2. 1 не выходе дешифратора переместилась на выход номер 2. Т.о. прошёл всё да в пизду. \\

	Дешифратор кода определяет какие именно импульсы нужны для реализации команды. Объединяет всё блок управления операциями (что и когда). Из него во все стороны и формируются i-е управляющие сигналы, подключающиеся к устройствам. 
\end{enumerate}


Вопросики:\\
Какие времена существуют: физическое и дискретное.\\
Какое время в ЭВМ: дискретное время.\\
Какая связь дискретного и физического времени? Дискретное время - время физическое разрубленное на кванты. \\
Что происходит между $t$ и $t_1$? Между $t$ и $t_1$ как раз и происходят все процессы в ЭВМ. \\
Как связаны переходные процеесы с интервалом физического времени между t и t+1... - тактовые импульсы - импульс, которые продвигает дискретное время к t+1. 
Какая характеристика определяет что-то там - Период периодического тактового сигнала. 
Обратная ей - частота
Ограничение для увеличения частоты - те самые переходные процессы. Если сильно уменьшить интервал t и t+1, так что логические синаоы не успеют совершить свой переход, то всё поломается. Именно это определяет максимальную частоту. 
Как работает классическая ЭВМ? - Циклически - посторение 1 и того же набора действий. \\
ЭВМ повторяет тот набор действий, которыей необходим для выполнения команды - цикл выполнения команд. 

3адресЭВМ - значит, что она имеет в своей части 3 адреса поля. 
Как связаны формат команды и РК - неразрывно, тк формат команды и регистр команды обсолютно одинаковы. Ведь оказавшись записанно в этот регистр тот, число записанное в памяти обретает смысл его формата. 
Счётчик команды - хранит адрес следующей команды и формирует адрес следующей команды путём инкремента на длинну текущей команды. а) хранит адрес следующей команды б) формирует его.
Кешифратор КОП - определаяет какой набор сигналов необходим для выполнения текущей команды. 
Датчик сигналов - поставляет тактовые импульсы в развёрнутой картине (развёртка во времени событий происходящих внитри цикла выполнения команды)
Блок управление операции - взяв с одной стороны что сделать с другой стороны в какие момены надо это сделать, соединяет одно с другим и пораждает управляющие сигналы. 

Структура регистра команд:
\begin{itemize}
	\item Код операции - код операции команды
	\item a -адрес первого операнда. 
	\item b - адрес второго операнда.
	\item с - адрес куда записать результат. 
\end{itemize}

\section{20.11.2025 Вычислительный процесс в классической трёхадресной ЭВМ.}
\iffalse 
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/3AdressEVM/Вычислительные процессы}
\end{center}
\fi 
Начальное условие и предположения:
\begin{itemize}
	\item программа и операнды находятся в оперативном запоминающем устройстве (ОЗУ)
	\item адрес ячейки ОЗУ, в которой находится выполяемая команда, нахоится в счётчике команд 
	\item команда считывается за одно обращение к ОЗУ; 
	\item команда, операнды и результат используют прямую адресацию к памяти. 
\end{itemize}

\begin{center}
	\includegraphics[width=0.7\linewidth]{images/3AdressEVM/Блок-схема}
\end{center}

\textbf{Линейная команда} - команда, которая выполняет преобразования данных операндов. 
Команда ветвления - никак не изменяют данные и используются исключительно для изменения выполнения порядка исполнения команд программы.


\begin{center}
	\includegraphics[width=0.7\linewidth]{images/3AdressEVM/1.1}
\end{center}

\iffalse
Из счётчика команд, находящаеся там число передаётся на вход памяти, а именно на его регистр адреса. С которого уже внутри ОЗУ дешифрируется и изыскивается та самая ячейка. которую представляет это чисо. Содержимое передаётся на выход ОЗУ по 1 магистрали её содержимое передаётся в УУ, конкретно в регистр команды. Попав на регистр команды это число представляется в команду, которая несёт смчсл в соответствии с ёё форматом и содержимое поля КОП передаётся в лешифратор кода операции, смысл определить что это за команда была считана из памяти. 
1 этам - чтение команды из ОЗУ по адресу, находящемуся в счётчике команд. 
Определеили что за команда и что она линейная, а в ней в соответствии и ... ЭВМ находятся 3 опреанда куда записать резульатат
2 этап:
Из поля 1 адреа операнда а, это число передаётся на решгистр адреса ЗУ. Попав на регистр адреса это число используется для определения ячейки с этом адресом и на выход содержимое ячейки. Суть 1 первый операнд, которые по 2 магистрали передаётся в АЛУ, конкретно в регистр 1 операнда (регистр опреанда А). Произведено чтение опренада из ОЗУ по адресу а. 
этап 3
Из поля б регистра команды извлекается число и передаётся в ОЗУ, выбирается ячеёка и содержимое на выход ОЗУ, интерпретируется как 2 операнд и передаётся на АЛУ в регистр операнда Б. Чтение 2 операнда из адреса б. 
этап 4 
Выполнение операции в АЛУ с получанием результата, которые записывается в регистр результат и признаков результата, которые помещается в регистр признака рзультата. Выполнение опреации в АЛУ и помещение в регистры. 
5 этап 
Из поля c реистра команды извлекается записанное там число и передаётся на регистр адреса оперативной памяти, дэшифрируется и одовременно с этим с регистра результата по информационной магистрали на вход для записи ОЗУ передаётся результат. Адрес, результат + сигнал приводит к тому, что результат записывается по адресу, указанному в поле c. 
6 этап
Выполняется инкремент содержимого счётчика команды на длинну команды. В нашем слуае длинна - 1. В результате чего в счётчеке команд получаеся адрес следующей ячейки, в которой лежит следующая команада. 
Дальше всё повторяется
Нет средства понять, что команды кончились. Цикл не кончается никогда, пока включено питание. Не существует команды остановок. 

Автомат который всё что умеет делать - выполнять этот цикл. 

ОДНАКО


Это цикл только ё типа команды, а типов команды 2. 
Посмотрим 2 вариант. 

Этап 1 
Обсолютно идентичный 1 этапу предыдущему. 
Когда дешифратор определеит, что команда - коамнда перехода алгоритм выполнения меняется. 
В это случае формат команды интерпретируется иначе, в нём помимо кода операции появляется 1 поле (а) - А перехода, подразумевается, что там адрес ячеёки памяти, в которой команда, куда осущетвить переход. Дальше разные вариенты 
Переход безусловный
Вэтом случае адрес перехода заносится в счётчик команд замещая собой его содержимое. Дальше в следующем цикле по этому адресу считывается следующая команда и по новой. 
Команда условного перехода. 
Она содержит ещё 1 поле - поле опренда b - маска. Маск - шаблон значений регистра признаков результата и дальше этот шаблон передаётся на схему сравнения, в котором происходит сравнение значений регистров признаков результата (их называют лагами) с образцом в поле маски. Либо совпадает либо не совпадает. Если не совпадает, то никаких действий не происходит. Счётчик инкрементируется на 1, указывает на след команду и по новой 
Если совпадает, то адрес переход загружается в счётчик команд, замещая собой его содержимое.



Что такое хорошо что такое плохо:
Хорошо: Она работает. Полноценнофункциональнаё. 

\fi 


\begin{enumerate}
    \item \textbf{Выполнение линейной команды}

    \begin{enumerate}
      \item \textbf{Этап 1. Выборка команды} \\
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/3AdressEVM/1.1}
\end{center}
\begin{itemize}
	\item 
      Из счётчика команд число (адрес текущей команды) передаётся на вход памяти~--- в её регистр адреса. Внутри ОЗУ этот адрес дешифрируется, и из соответствующей ячейки извлекается команда. Её содержимое по первой магистрали передаётся в управляющее устройство (УУ), а именно~--- в регистр команды.  
      Попав в регистр команды, это число интерпретируется как команда в соответствии с её форматом. Поле кода операции (КОП) направляется в дешифратор кода операции, чтобы определить, какая именно команда была считана из памяти.

      \item \textbf{Кратко:} Чтение команды из ОЗУ по адресу, хранящемуся в счётчике команд.
        \item Определено: команда линейная, в формате трёхадресной ЭВМ~--- содержит три адресных поля: два операнда-источника (A и B) и один адрес результата (C).

\end{itemize}
      \item \textbf{Этап 2. Чтение первого операнда (A)} \\
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/3AdressEVM/1.2}
\end{center}
\begin{itemize}
	\item 
      Из поля A регистра команды адрес передаётся в регистр адреса ОЗУ. По этому адресу выбирается ячейка, и её содержимое (первый операнд) поступает на выход памяти.  
      Операнд передаётся по второй магистрали в АЛУ~--- в регистр первого операнда (регистр A).

      \item \textbf{Кратко:} Чтение первого операнда из ОЗУ по адресу A м передача его в АЛУ.

\end{itemize}
      \item \textbf{Этап 3. Чтение второго операнда (B)} \\
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/3AdressEVM/1.3}
\end{center}
\begin{itemize}
	\item 
      Из поля B регистра команды извлекается адрес, который передаётся в ОЗУ. Выбирается соответствующая ячейка, её содержимое интерпретируется как второй операнд и передаётся в АЛУ~--- в регистр операнда B.

      \item \textbf{Кратко:} Чтение второго операнда из ОЗУ по адресу B и передача его в АЛУ.

\end{itemize}
      \item \textbf{Этап 4. Выполнение операции} \\
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/3AdressEVM/1.4}
\end{center}
\begin{itemize}
	\item 
      АЛУ выполняет операцию над двумя операндами. Полученный результат записывается в регистр результата, а признак результата (например, ноль, переполнение, перенос и т.д.)~--- в регистр признаков результата.

      \item \textbf{Кратко:} Выполнение операции в АЛУ; результат и его признаки сохраняются в регистрах.\\\\
	      Скорее всего Новиков должен был рассказать тут и про формиование управляющих сигналов, но у меня этого нет. Кажется этого вообще не было.
\end{itemize}
      \item \textbf{Этап 5. Запись результата} \\
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/3AdressEVM/1.5}
\end{center}
\begin{itemize}
	\item 
      Из поля C регистра команды извлекается адрес, который передаётся в регистр адреса ОЗУ и дешифрируется. Одновременно результат из регистра результата передаётся по информационной магистрали на вход записи ОЗУ.  
      По приходу управляющего сигнала записи результат сохраняется в ячейке памяти по адресу C.
	\item \textbf{Кратко:} Запись результата в ОЗУ. 

\end{itemize}
      \item \textbf{Этап 6. Обновление счётчика команд} \\
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/3AdressEVM/1.6}
\end{center}
\begin{itemize}
	\item 
      Содержимое счётчика команд увеличивается на длину текущей команды (в данном случае~--- на 1). Теперь счётчик указывает на следующую команду в памяти.

\item \textbf{Кратко:} инкремент счётчика команды. 
\end{itemize}
\end{enumerate}
На это цикл заканчивается и всё начинается заново.\\\\
    \textbf{Особенности:}
	\begin{enumerate}
      \item В системе \textbf{нет механизма}, позволяющего определить конец программы.
      \item Цикл выполнения \textbf{не прерывается}, пока включено питание.
      \item \textbf{Команды остановки не существует.}
      \item Машина представляет собой автомат, предназначенный \textbf{только для бесконечного выполнения этого цикла}. 
	\end{enumerate}
    \textbf{Примечание:} описанный выше цикл справедлив \textbf{только для линейных команд}.  
    \item \textbf{Выполнение команды перехода}
      В трёхадресной ЭВМ существует \textbf{второй тип команд}~--- команды перехода.

    \begin{enumerate}
      \item \textbf{Этап 1. Выборка команды} \\
      (Полностью идентичен этапу 1 для линейной команды.)

      \item \textbf{Этап 2 (не факт). Распознавание команды перехода} 
	\begin{itemize}
	      	\item Если дешифратор определяет, что команда~--- \textbf{команда перехода}, её формат интерпретируется иначе:
      \item Помимо КОП, используется \textbf{одно адресное поле (A)}~--- \textbf{адрес перехода}. Подразумевается, что по этому адресу находится команда, с которой должно продолжиться выполнение.

      Далее возможны два варианта:
\begin{enumerate}
      \item \textbf{Безусловный переход} \\
      Адрес из поля A \textbf{непосредственно загружается в счётчик команд}, замещая его текущее значение.  
      На следующем цикле выборки команда будет считана уже \textbf{по новому адресу}.

      \item \textbf{Условный переход} \\
      Команда содержит \textbf{два поля}:\\
      \textbf{A}~--- адрес перехода,\\
	\textbf{B}~--- \textbf{маска} (шаблон значений регистра признаков результата).

\end{enumerate}
      Процесс:
      \begin{enumerate}
        \item Маска передаётся на схему сравнения.
        \item В схеме сравниваются текущие значения \textbf{флагов} (признаков результата) с маской.
        \item \textbf{Если совпадение есть}~--- адрес A загружается в счётчик команд.
        \item \textbf{Если совпадения нет}~--- счётчик команд инкрементируется на 1 (как при линейной команде), и выполнение продолжается со следующей команды.
      \end{enumerate}
	\end{itemize}
    \end{enumerate}


    \item \textbf{Что хорошо:}
      \begin{itemize}
        \item Машина \textbf{работает} и \textbf{полноценно функциональна}.

      \item \textbf{Что следует учитывать:}
      \item Отсутствие команды останова требует явного управления потоком программы (например, бесконечным циклом или переходом на начало).
        \item Все поведенческие особенности определяются \textbf{форматом команд} и логикой дешифрации КОП.
    \end{itemize}
  \end{enumerate}

\section{Двухадресная ЭВМ}
Структура
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/2AdressEVM/0}
	\end{center}
\begin{enumerate}
	\item 
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/2AdressEVM/1}
	\end{center}
	\item 
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/2AdressEVM/2}
	\end{center}
	\item 
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/2AdressEVM/3}
	\end{center}
	\item 
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/2AdressEVM/4}
	\end{center}
	\item 
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/2AdressEVM/5}
	\end{center}
	\item 
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/2AdressEVM/6}
	\end{center}
\end{enumerate}

Когда 2 лучше, чем 3?  
Что такое хорошо, а что такое плохо, когда речь идёт об ЭВМ?  
Что мы убрали и почему оно работает?  
Что со временем в этом случае?  
Как организовать регистр адреса так, чтобы в него входило 4 стрелочки?

\section{Схемотехническая база ЭВМ}

\textbf{Схемотехника} — наука о том, как создавать электронные устройства путём объединения компонентов в изделия с помощью схем.  
\textbf{Схема} — будем понимать как совокупность элементов и связей между ними.

Схемы создаются двумя способами:
\begin{enumerate}
	\item Берётся то, на чём писать, и то, чем писать, и создаётся схема. При этом компоненты, являющиеся частями этой схемы, изображаются с помощью условных графических обозначений, а связи между ними — линиями, соединяющими входы и выходы.
	\item Описание схем на специальных языках. У всех таких языков одно и то же предназначение: HDL (Hardware Description Language). Мы используем VHDL. Эти языки чем-то напоминают языки программирования, но разработка устройств не превратилась в программирование. Это лишь способ воплотить идеи разработчика.
\end{enumerate}

Вся схемотехника состоит из двух частей:
\begin{enumerate}
	\item Комбинационная схема
	\item Триггерная ячейка (триггер)
\end{enumerate}

С комбинационной схемой мы уже знакомы. Это не что иное, как материализация в виде схемы функций алгебры логики.  
Со второй частью (триггером) дело обстоит иначе: он изготавливается из того же материала, что и комбинационные схемы, а потому следует пробежаться по всему, что мы изучили.

\subsection{Комбинационная схема}

Комбинационная схема — это такая цифровая электронная схема, состояние выходов которой (сигнал) зависит \textbf{только} от состояния её входов (сигналов).

Следствия:
\begin{enumerate}
	\item В такой схеме отсутствуют обратные связи (нет соединения выходов со входами).
	\item В ней полностью отсутствуют элементы памяти.
\end{enumerate}

\textbf{Комбинационная схема} — это реализация в виде схемы функции алгебры логики. Следовательно, она возникает в результате решения задачи синтеза логической схемы.

Синтез схемы состоит из следующих этапов:
\begin{enumerate}
	\item Запись логической функции, описывающей желаемое поведение схемы, в каноническом виде (с помощью таблицы истинности). Возможны два варианта: полностью и неполностью определённые логические функции. Преобладают неполностью определённые логические функции.
	\item Минимизация (оптимизация) логической функции.  
	\textbf{Оптимизация} — получение наилучших характеристик устройства при заданных ограничениях.
	\item Построение схемы цифрового устройства (логической схемы).
	\item Верификация (проверка). Выполняется с помощью различных методов моделирования.  
	\textbf{Модель} — аналог объекта в природе, реализованный в других физических явлениях. Наибольшее количество ошибок выявляется на этапе функционального моделирования.  
	\textbf{Моделирование} — создание модели для проверки логических функций. Все элементы считаются идеальными, длины связей не учитываются, поскольку предполагается, что скорость распространения сигнала бесконечно велика. После этого обычно происходит привязка к элементной базе либо отладка на макете. Когда всё готово, изготавливаются рабочие экземпляры.
\end{enumerate}

При отладке устройства решается задача анализа: определить, какую функцию оно реализует.

Комбинационные схемы строятся из логических элементов.  
\textbf{Логический элемент} — устройство, реализующее операции над сигналами, несущими значения логических переменных. Значения логических переменных представляются в виде электрических потенциалов.  
\textbf{Импульс} — изменение электрического сигнала, несущего значение логической переменной, сначала на противоположное, а затем обратно. Импульс без времени невозможен.

Модель элемента состоит из его логической функции и задержки. Логические элементы реализуются с помощью транзисторов (ТТЛ или полевые транзисторы).

Основные логические элементы:
\begin{itemize}
	\item И
	\item ИЛИ
	\item НЕ
	\item Штрих Шеффера
	\item Стрелка Пирса
\end{itemize}

Они должны составлять функционально полный базис.

Однако некоторые схемотехнические решения встречаются настолько часто, что выпускаются в виде готовых микросхем, библиотечных компонентов для систем моделирования или в виде IP-ядер (Intellectual Property core).

\subsection{Комбинационные схемы средней степени интеграции}

\subsubsection{Дешифратор}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/Дешифратор}
\end{center}
\textbf{Дешифратор} — комбинационная схема, осуществляющая преобразование позиционного двоичного кода в унитарный код.  
\textbf{Позиционный код} — представление числа в позиционной системе счисления.  
\textbf{Унитарный код} — код, содержащий ровно одну единицу. Его величина определяется десятичным номером разряда, в котором находится эта единица. Входы обозначаются буквой \(A\); их всего \(k\) штук. Во всех случаях, не указанных в таблице, на выходах возвращается 0.

Основное применение дешифратора — решение задачи выбора.  
Входы дешифратора соединяются со входами разрешения устройств.

\subsubsection{Мультиплексор}
\textbf{Мультиплексор} — комбинационная схема, реализующая цифровой коммутатор по схеме «много в один» (много входов → один выход).
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/Мультиплексор}
\end{center}
Устройство: имеется набор информационных входов и набор адресных (селекторных) входов, а также один выход. Мультиплексор осуществляет цифровую коммутацию. Таким образом, он является переключателем, управляемым посредством указания номера на адресных входах.

\subsubsection{Демультиплексор}
\textbf{Демультиплексор} — цифровой коммутатор, работающий по схеме «один вход → много выходов».
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/Демультиплексор}
\end{center}

\subsubsection{Сумматор}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/Сумматор}
\end{center}

\subsubsection{Мультиплексная схема передачи данных}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/Мультиплексорная шина}
\end{center}
На входе — мультиплексор, на выходе — демультиплексор; между ними — одна единственная линия связи.

Необходимо синхронизировать значения на адресных (селекторных) входах мультиплексора и демультиплексора.

Технология — \textbf{мультиплексирование данных} — передача через ограниченный канал за счёт разделения во времени. Осуществляется параллельно-последовательное и последовательно-параллельное преобразование кодов.

\subsection{Триггерные (последовательностные) схемы}

\textbf{Триггерные схемы} — последовательностные схемы (схемы с памятью).  
\textbf{Свойство памяти} — способность сохранять своё состояние во времени.

Мы будем обозначать состояние как \(Q\); оно может принимать два значения: логическая 1 и логический 0.  
Время в ЭВМ дискретно (квантовано), поэтому, когда мы говорим о времени, к состоянию следует добавлять зависимость от времени: \(Q(t)\). Также используется обозначение \(Q(t+1)\) — значение в следующий дискретный момент времени.

Если \(Q(t) = Q(t+1)\), это означает, что состояние не изменилось, то есть оно сохранилось. Это позволяет утверждать, что память присутствует: состояние запомнилось. — Новиков Г. Г.

В зависимости от того, как трактуется момент перехода от текущего состояния к следующему, схемы с памятью делятся на два класса:
\begin{enumerate}
	\item Синхронные
	\item Асинхронные
\end{enumerate}

Если представить схему в виде графического обозначения, то у неё всегда будет некоторый набор входов и хотя бы один выход. Состояние устройства обычно обозначается значением логической переменной на его выходе.

Если в схеме присутствует хотя бы один специальный вход (обозначаемый \(C\)) — \textbf{схема синхронная}.  
Если такого специального входа нет — \textbf{схема асинхронная}.

Что такое триггер (переключатель)? Это устройство, имеющее два устойчивых состояния (бистабильность), два взаимно инверсных выхода и управляющие входы (обозначаются \(T\)).

\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/Тригер1}
\end{center}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/Тригер2}
\end{center}

\subsubsection*{Вопросики 04.12.2025}
Чем отличается $T$ от $T+1$ - T - сейчас, $T+1$ - следующий момент времени. 
Что может вызвать переход времени из состояния T в состояние $T+1$ - 2 варианта, которые определяют 2 вида схем: синхронные и асинфроные. \\
Для асинхронных: любое изменение сигналов на любом входе. Для синхронных: всё то же самое, но на определённом конкретном коде (синхронизация). Синхронно - одновременно. \\
Что такое последовательностные схемы? - сейчас состояние было, сейчас пришло воздействие, а изменение будет только в следующий момент времени. 
Простейший представитель последовательных схем? - тригер.

\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/}
\end{center}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/}
\end{center}

\subsubsection{Обобщённая структура триггрной схемы}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/}
\end{center}

такое преобразование - комбинационняа схема, которая превращает всё в 2 функции, которые работают с R, S входами. Те кто 

\subsubsection{Элементарные тригерные схемы. Основные типы.}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Схемота/}
\end{center}

\begin{enumerate}
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/Схемота/}
	\end{center}
	\item T - тригер. T от слова togle (перевёртыш). Если проанализировать таблицу переходов, то мы понимаем, что когда приходит 0, то он в состоянии хранения, а при 1 формируется НЕ Q(t). Кроме того есть вход C - синхронизация. Как правило для коректного выполнения операции смысловые входы должны быть установлены перед синхронизацией. То минимальное время за которое это должно быть установлены сигналы на управляющий входах - время предустановки. 
	\item RS(SR) - триггер. Рассмотрен выше. 
	\item JK - триггер. J и K ведут себя так же как и S R. Отличие в запрещённой комбинации. Вместо неё в JK тигер типа T. Запрещённая комбинация приводит к изменению состояния на противоположное. 
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/Схемота/}
	\end{center}
	\item D - триггер. Delay (задержка) В момент T+1 будет на выходе то, что было на входе в момент времени T. Тоесть хранение. 
	\item DV - триггер. D - триггер с входом разрешения. До тех пор пока на входе V присутствует 0 тригер находится в состоянии хранения. Когда 1, он повторяет D триггер. 
\end{enumerate}

\subsection{Регистры}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/Регистры/}
\end{center}
Берём тригеры и делаем из них регистры. Кроме того регистры могут 
\begin{enumerate}
	\item Хранение (количество тригеров = количеству разрядов)
	\item Сдвиг (у регистров сдвига) 
	\item Универсальные регистры - регистры способные выполнять микрооперации
\end{enumerate}

Примеры регистров:
\begin{itemize}
	\item Регистр хранения
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/Схемота/Регистр хранения}
	\end{center}
	CLS - вход асинхронной команды очистки в 0. \\
	Состоит из 4 D тригеров. 
	\item Регистр сдвига
	\begin{center}
		\includegraphics[width=0.7\linewidth]{images/Схемота/Регистр хранения}
	\end{center}
	
	SLI - Shift Left Input - вход последовательного приёма данных при сдвиге влево. Название означает, что происходит сдвиг влево (в сторону старших разрядов/сверху вних по картинке). \\
	D0, D1, D2, D3 - входы для параллельной загрузки (тоже самое у регистра хранения). \\
	L - 1 разрешает load (загрузку) если 0, то выполняется функция сдвиг. \\
	CE - chip enable - разрешающий работу вход. 
	C - вход с треугольничков внутрь значит, что все мериприятия происходят по команде синхроимпульса. \\
	Состоит из 4 T-триггеров и 4 мультиплексоров, объединённых селекторным входом (из L). 
\end{itemize}

\subsection{Счётчик}
Устройство последовательностного типа, обладающее способностью считать. Предназначено для подсчётов импульсов, приходящий на специальный вход (счётный вход) устройства. \\
Помимо подсчёта это устройство
\begin{itemize}
	\item Хранит то, что насчитала
	\item Отображается на выходах в заданном коде (кодировке). 
	\item Имеет конечное количество состояний, тоесть чисел, которые в нём могут быть, определяемое количество разряда счётчика. Достигнув максимального значения счётчик сбрасывается. Количество неповторяющихся состояний - модуль счётчика (модель пересчёта). 
		Из графа однозначно следует, что следующее состояние счётчика зависит только от предыдущего состояния и не зависит ни от чего пришедшего из вне.
	
\end{itemize}

\section{Лекция от 18.12.2025}

Что такое магистраль?
Что такое сегментация?
Какая проблема привела к том, что пришлось сделать память сигентированной?
Одна из проблем: нужно из нано мира передать информацию в микро мир. 
Что за приём позволояет через узкое горлышко протащить что-то крупноразмерное? - мультиплексирование. 
Как сделать команду в Itnel 8086, которая переносила бы 1 байн из одной ячейки памяти в другую? - сдвиг. Правильный ответ никак (или 2 команды из ячейки в регистр и из регистра в ячейку)
Какие бывают форматы команд двухадресных в этом устройстве: 
РР - один операнд в 1 регистре второй в другом регистре (регистр регистр)

РС - регистр storage. Один в регистре второй в памяти. 
СС - обы в оперативной памяти. \textbf{Этого формата нет} в процессоре Intel 8086. 
Обязательно один из операндов находится \textbf{в регистре}.


Наш ребёнок получился мертворождённым. Нужно добавить software чтобы устройство стало полезным. 
\subsection{Software. Програмное обеспечение.}
Без апаратного обеспечения тоже абстрактна и бесполезна. 
Без програмного обеспечения железка - дорогой камень.
Вместе очень классная вещь. 
\textbf{Всё что можно реализовать програмно можно реализовать апаратно и наоборот.}

Предварительные условия: 
\begin{enumerate}
	\item Программа и данные уже находсятся в оперативной памяти. 
	\item Адрес первой команды программы уже находится в счётчике. (магия) 
\end{enumerate}
Теперь разберёмся как оно туда попало. 
Счётчик состоит из столькх тригерой какой разрядности необходим счётчик. Для адреса команды необходимо логорифм двоичный от области памяти. Без питания с тригерами ничего не происходит. А при включении питания определяется абсолютно случайно. 

Получается, что нам необходимо загрузить программу и каким-то образом указать счётчику команд с какой команды выполнять программу. 

Делается это следующим образом:
В ООП (RAM): 
по Нейману рядом находится \textbf{энергонезависимая} память. Осталось как-то доставить в ООП. Ключевая роль у источника питания. Как только он приходит в рабочий режим и достигает нужных напряжений он выдаёт на специальном выходе импульс, который называется общесистемный сброс. Этот импульс передаётся в те места, где необходимо установить всё в жедаемое исходное состояние. Так же можно вручную пустить этот сигнал кнопочкой \texttt{resrt}. Главное что делает этот импульс: добирается до сччётчика команд и как правило попадает на его вход R (CLR). После чего счётчик оказывается в состоянии все 0. 
Дальше нужно поработать с памятью. Отрезать кусочек и сдлеать его не RAM, а ROM (Read only memory). Как правило абсолютно энергонезависимая. Именнто туда с ячейки 0 укладывается та программа, которая должна запуститься первой. А дальше то, что написано в тексте этой программы. (Слазить в энергонезависимую память и загрузить в ООП всё что надо). Base Input Output System (BIOS). 
Таким образом решается проблема откуда в памяти появляется программа (данные) и заполняется счётчк. 

\subsection{Организация вычислительного процесса}

\begin{enumerate}
	\item Иерархия програмного обеспечения
	\item Понятие операционная система
	\item Монопольный и Мультипрограмный режим работы ЭВМ. 
	\item Распределение ресурсов
	\item Виртуализация и защита памяти 
	\item Конвейнерная организация ЭВМ
\end{enumerate}

\subsubsection{Иерархия програмного обеспечения.}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/hierarchy}
\end{center}
Драйвер - программа, которая управляет тем конкретным устройством, для которого она сделана и реализует 2 интерфейса: с одной стороны устройство, с другой следующий слой програмного обеспечения. Суть: разную специфику разных переферийных устройств привести к стандарту. 
Основная управляющая программа - операционная система. Смысл: обеспечение и максимальный комфорт жизнедеятельности других программ. 
Прикладное програмное оеспечение: то что даёт какую-то пользу. Где-то между ними живут ещё большое количество программ, которые можно называть инструметальными програмами (главным образром для написание, отладки и разработки других програм. БД, ЯП, системы автоматизации проектирования, ...) 

Специфика организации этого определяет как стиль общения так и технические характеристики вычислительной системы. 

Большую часть состаяляют интерфейсы системы общения пользователя и системы. (перфокарты, голос, камеры и т.д.) 

Вся эта деятельность ОС сводится к несложным вещам:
\begin{enumerate}
	\item Процесс
	\item Ресурс
\end{enumerate}

\subsubsection{ОС}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/ОС}
\end{center}

\subsection{Расределение ресурсов между процессами.}
Есть некий процесс, который ждёт ответ от доступьзователя (захватил экран и клавиатуру). Тоесть другие процессы не могут получить к ним доступ. Решение:
Например выдавать доступ на лимитированный промежуток времени. 

\subsubsection{Процедуры распределения ресурсов мельтипрограмной ЭВМ.} 
Например FIFO (очередь) 
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/FIFO}
\end{center}
В столовой тянемся к ресурсом, но кто первый встал тому и тапки. Но есть процессы у которых приоритет выше других. Его пропускают вперёд. Но если авторитетных много, то через одного. 
LIFO. Первый вылетает поседний патрон. Stack. 
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/LIFO}
\end{center}
\subsection{Дисциплины с многими очередями.}
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/Multic}
\end{center}

\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/Priority}
\end{center}

\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/Priority2}
\end{center}
\subsection{Виртуализация и защита памяти.}
Попытка решения 1 из основных вопросов философии. Память мы хотим быструю и бесконечную, а технологии не позволяют :-(((
\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/Virt}
\end{center}

\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/Virt2}
\end{center}

\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/Defend}
\end{center}

\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/Defend2}
\end{center}

\begin{center}
	\includegraphics[width=0.7\linewidth]{images/End/Defend3}
\end{center}

\end{document}

